{
  "version": 2,
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "functions": {
    "api/**/*.ts": {
      "includeFiles": "src/**",
      "maxDuration": 30
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/api/index.ts" }
  ]
}
